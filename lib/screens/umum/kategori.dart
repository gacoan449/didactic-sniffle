import 'package:flutter/material.dart';
class HalamanKategori extends StatelessWidget {const HalamanKategori({super.key});@override Widget build(BuildContext context)=> Scaffold(appBar: AppBar(title: const Text('Kategori')));}
